//A simple function that checks if the arg is true or false

const booWho = (arg) => {
  if (arg === true) {
    return true;
  } else if (arg === false){
    return true;
  } else {
    return false;
  }
}

console.log(booWho());
